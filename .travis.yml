language: python

python:
  - 3.5
  - 3.6

install:
  - pip install -r requirements.txt
  - pip install tox-travis
  - python setup.py install

script:
 - py.test --timeout=9 --duration=10 --cov --cov-report=html

after_success: 
 - coverage report
 - coveralls